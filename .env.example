# Copy to .env (kept 0600, gitignored) and fill in what you need.
# All vars are optional — the server runs with just the defaults below.

# SQLite db path, opened/created on startup.
SWITCHYARD_DB=switchyard.db

# HTTP port the server listens on.
PORT=3300

# Directory where issue attachments (attach_file) are stored on disk.
ATTACHMENTS_DIR=./attachments

# Base URL used to build links (e.g. the login link printed by `mint-login`).
SWITCHYARD_URL=http://localhost:3300

# HMAC secret for verifying inbound GitHub webhook deliveries
# (POST /webhooks/github). Also set as the webhook's secret on the GitHub
# side. Deliveries are rejected with 501 if this is unset.
GITHUB_WEBHOOK_SECRET=

# Hours an issue can sit claimed with no activity before the webhook
# dispatcher releases the claim back to todo. Defaults to 4 when unset
# or invalid.
STALE_CLAIM_HOURS=4

# Set to 1 to mark the session cookie Secure (HTTPS-only). Leave unset when
# serving over plain HTTP (e.g. Tailscale IP), or logins will not stick.
SWITCHYARD_COOKIE_SECURE=

# --- `npm run affirm` (the syd affirm client CLI, SYD-242 phase 2) ---
# These are read by src/affirm-cli.ts on the HUMAN's machine, not by the server.
# The CLI talks HTTP to the tracker; it does not open the database.

# Your own human bearer token, used as TRANSPORT auth only. Holding it is not
# sufficient to affirm a gated action — the hardware signature is the actual
# authorization, so this proves who is asking, not who approved.
#
# This is the token human-only acts use: affirming, and `scripts/syd.ts`
# (pr-link confirm, resolving a deviation). Name it after the person, not the
# type — a variable called "human token" holding an infra actor's credential is
# how a poller ends up passing gates meant for a person.
SWITCHYARD_HUMAN_TOKEN=

# The dispatch worker's own AGENT token. Distinct from every variable below:
# agents are refused by the delivery and GitHub-ingestion endpoints on purpose.
SWITCHYARD_TOKEN=

# Trusted-infra tokens, one per least-privilege `service` actor (SYD-213).
# They post observations and outcomes; they can never author or modify issues.
# Create with: switchyard <db> add-actor <name> service
SWITCHYARD_GITHUB_POLLER_TOKEN=
SWITCHYARD_DELIVER_POLLER_TOKEN=

# Path to the FIDO/security key that signs affirmations.
# Defaults to ~/.ssh/id_ed25519_sk. Generate one with:
#   ssh-keygen -t ed25519-sk -O resident -O verify-required -C "syd-affirm:<you>"
# then enrol its .pub with: switchyard <db> add-affirm-key <you> <key>.pub
SWITCHYARD_AFFIRM_KEY=

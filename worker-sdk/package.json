{
  "name": "switchyard-worker-sdk",
  "private": true,
  "type": "module",
  "description": "Isolated deps for the Claude Agent SDK runner (runner: \"sdk\"). Separate package on purpose: the SDK peer-depends on zod@4 while the main app is on zod@3, and the server Docker image should never ship the SDK. Install with: npm install --prefix worker-sdk",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.204",
    "zod": "^4.0.0"
  }
}
